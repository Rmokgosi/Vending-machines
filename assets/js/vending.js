//declare variables
var itemNo = new Array("1", "2", "3", "4", "5", "6", "7"); //item number array
var itemPrice = new Array("11", "11", "12", "12", "8", "8", "9"); //price of item array

var amt = Number(document.getElementById(amount).value); //inputAmount
var itemNum = Number(document.getElementById(item_num).value);//inputItemNumber
var price ;

function getItemPrice()
{  if () }


// Clear field on a input box when clicking on the Refreah button
function clearField(){
	document.getElementById().value = ;
}

const reloadtButton = document.querySelector("#reload");



function getItem(){

}

// Reload everything:
function reload() {
    reload = location.reload(); }
/*Total amount = takes the for amount entered and subtract it from the inputAmount

*/
